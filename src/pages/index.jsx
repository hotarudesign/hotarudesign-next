import Head from "next/head";
import { motion } from "framer-motion";
import { About } from "src/component/About";
import { Feature } from "src/component/Feature";
import { Footer } from "src/component/Footer";
import { Header } from "src/component/Header";
import { Mv } from "src/component/Mv";
import { Works } from "src/component/Works";
import { useCallback, useEffect, useState } from "react";
import { client } from "src/libs/client";
import Link from "next/link";

const Home = (props) => {
  return (
    <motion.div
      initial={{ opacity: 1, x: 0 }}
      animate={{ opacity: 1, x: 0 }}
      exit={{ opacity: 0, x: -500 }}
      transition={{ ease: "easeInOut", duration: 0.5 }}
    >
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Header />
      <Mv />
      <Feature />
      <About />
      <ul>
        {props.contents.map((content) => {
          return (
            <li key={content.id}>
              <Link href={`/works/${content.id}`} legacyBehavior>
                <a>
                  {content.title}
                  {/* <div className={classes.item__txt}>
                    <h3 className={classes.item__ttl}>{content.title}</h3>
                    <p className={classes.item__category}>{content.category}</p>
                  </div> */}
                </a>
              </Link>
            </li>
          );
        })}
      </ul>
      <Footer />
    </motion.div>
  );
};
export const getStaticProps = async () => {
  const data = await client.getList({ endpoint: "works" });
  return {
    props: data,
  };
};

export default Home;
